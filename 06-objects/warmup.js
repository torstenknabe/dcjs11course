
// 1) Write a function that generates a random integer between 1 and a max number, which will be a parameter

function generateRandomNum(max) {
    return Math.ceil(Math.random() * max)
}

// 2) Using the function from #1, write another function that generates X number of random numbers with a max of X, where X is a parameter passed into the function.
//    This function should then return the average of all of the random numbers.

function randomAvg(x) {
    
    sum / 

    return average
}